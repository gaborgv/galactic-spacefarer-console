{
  "name": "galactic-console",
  "version": "1.0.0",
  "description": "Galactic Spacefarer Adventure Management Console",
  "private": true,
  "dependencies": {
    "@cap-js/sqlite": "^2",
    "@sap/cds": "^9",
    "bcrypt": "^6.0.0"
  },
  "devDependencies": {
    "@cap-js/cds-test": "^1",
    "@sap/cds-dk": "^9",
    "mocha": "^11"
  },
  "scripts": {
    "start": "cds-serve",
    "watch": "cds watch",
    "deploy": "cds deploy --to sqlite",
    "deploy:fresh": "rm -f db.sqlite db.sqlite-wal db.sqlite-shm test.sqlite test.sqlite-wal test.sqlite-shm && cds deploy --to sqlite",
    "dev:stop": "lsof -ti :4004 | xargs kill 2>/dev/null || true",
    "dev:reset": "npm run dev:stop && npm run deploy:fresh && npm run watch",
    "test": "CDS_ENV=test mocha test/integration/**/*.test.js --timeout 30000"
  },
  "cds": {
    "requires": {
      "auth": {
        "impl": "srv/galactic-auth.js"
      },
      "db": {
        "kind": "sqlite",
        "credentials": {
          "url": "db.sqlite"
        },
        "data": [
          "db/seed"
        ]
      }
    },
    "[test]": {
      "requires": {
        "db": {
          "credentials": {
            "url": "test.sqlite"
          },
          "data": [
            "test/data"
          ]
        }
      }
    }
  },
  "engines": {
    "node": ">=18"
  }
}
